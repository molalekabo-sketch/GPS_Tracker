- [x] Update `public/index.html` telemetry panel: keep existing Voltage row, but add two new rows for Altitude and Speed.
- [x] Ensure UI parsing: altitude uses `data.altitude`, speed uses `data.speed` (numbers), with safe fallbacks.
- [ ] Ensure Leaflet popup reflects correct values if needed.
- [ ] Manual test: run `node server.js`, verify telemetry updates live and labels are correct.


