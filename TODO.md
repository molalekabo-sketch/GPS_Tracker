- [x] Make `public/index.html` overlays (telemetry/stats/control/format toggle) adapt to small portrait widths
- [x] Reduce/override min-width constraints on mobile so panels don’t overflow/crop
- [x] Add short-viewport handling (max-height media query) to avoid overlay overlap
- [x] Make `#control-panel` stretch full-width on mobile and allow wrapping buttons
- [x] Ensure no horizontal scrolling on mobile (set overflow-x hidden / box sizing)
- [ ] Smoke test: run `npm start`, open dashboard, verify overlays and map sizing on narrow/short viewports



